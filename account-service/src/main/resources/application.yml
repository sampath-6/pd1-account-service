server:
  port: 8080
  servlet:
    context-path: /pd1

spring:
  application:
    name: account-service

  profiles:
    active: qa   # activates application-qa.yml

  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration

logging:
  level:
    root: INFO